interface:
  display_name: "phatmux Browser"
  short_description: "Automate phatmux webview surfaces with snapshot/ref workflows."
  default_prompt: "Use this skill for browser automation via phatmux CLI: identify target surface, snapshot interactive refs, perform actions, wait for state changes, and re-snapshot to avoid stale refs."
