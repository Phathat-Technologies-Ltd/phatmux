interface:
  display_name: "phatmux Core"
  short_description: "Control windows/workspaces/panes/surfaces and routing with phatmux CLI."
  default_prompt: "Use this skill to inspect and manipulate phatmux topology: identify context, target handles, create/focus/move/reorder windows/workspaces/panes/surfaces, and use trigger-flash for visual confirmation."
