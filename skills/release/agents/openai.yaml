interface:
  display_name: "phatmux Release"
  short_description: "Prepare and publish phatmux releases from version bump to tag."
  default_prompt: "Use this skill to run the phatmux release workflow: choose version, generate user-facing changelog updates, bump versions, open and monitor release PRs, merge, tag, and verify release artifacts."
